const theme = require('./tailwind/theme')
const variants = require('./tailwind/variants')
const corePlugins = require('./tailwind/core-plugins')

module.exports = {
    prefix: '_',
    theme,
    variants,
    corePlugins,
}
